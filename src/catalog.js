export const SCRIPT_CATALOG = Object.freeze({
  hello: Object.freeze({
    latest: '1.0.0',
    latestPath: '/scripts/hello.sh',
    versions: Object.freeze({
      '1.0.0': '/scripts/hello/1.0.0.sh',
    }),
  }),
});
