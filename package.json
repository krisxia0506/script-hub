{
  "name": "script-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:shell": "sh -n public/scripts/hello.sh && sh -n public/scripts/hello/1.0.0.sh && sh -n public/scripts/setup-fenno-codex.sh && sh -n public/scripts/setup-fenno-codex/1.0.0.sh && sh -n public/scripts/setup-fenno-codex/1.0.1.sh",
    "check:worker": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "check": "npm test && npm run check:shell && npm run check:worker",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "wrangler": "4.129.1" }
}
